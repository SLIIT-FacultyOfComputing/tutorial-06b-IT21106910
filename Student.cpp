#include "Student.h"
#include <iostream>

// Assign studentId and name
void Student::assignDetails(int stdID, char name) {
  studentID = stdID;
  name = name;
}
// Display StudentId and Name
void Student::display(int stdID, char name) {
  studentID = stdID;
  name = name;
}
